# Backend (Render)
# Set this in Render dashboard → Environment → Add Env Var
PORT=3000
NODE_ENV=production

# Frontend (Vercel)
# Set this in Vercel dashboard → Project Settings → Environment Variables
VITE_BACKEND_URL=https://your-backend.onrender.com
