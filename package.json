{
    "name": "buzzer-app",
    "version": "1.0.0",
    "description": "Buzzer App monorepo root",
    "scripts": {
        "start": "cd backend && node server.js",
        "build": "cd backend && npm install --omit=dev",
        "postinstall": "cd backend && npm install --omit=dev"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}